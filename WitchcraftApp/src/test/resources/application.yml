spring:
  datasource:
    url: jdbc:mysql://${DATABASE_HOST:localhost}:3306/${DATABASE_NAME:witchcraft_test}?autoReconnect=true&useSSL=false&serverTimezone=UTC
    username: ${DATABASE_USERNAME:spring}
  jpa:
    show-sql: true

witchcraft.frontend.url: http://localhost:5000